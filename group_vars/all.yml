apps_path: /apps
gh_secrets: "{{ gh_secrets_json_base64 | b64decode | from_json }}"
gh_vars: "{{ gh_vars_json_base64 | b64decode | from_json }}"

deployment:
  allrensys:
    - fornex
  bashrc:
    - fornex
    - aeza
  bio:
    - aeza
  couchdb:
    - fornex
  grafana:
    - aeza
  node_exporter:
    - fornex
    - aeza
  prometheus:
    - aeza
  traefik:
    - aws
  telepovs:
    - aws
